# Copy this file to .env.local (gitignored) and fill in the values.
# afd360 loads .env.local automatically, with precedence:
#   process.env > .env.local > .env
#
# Never commit .env.local. Never paste these values into chat / PRs / logs.

AWS_ACCESS_KEY=
AWS_ACCESS_SECRET=
